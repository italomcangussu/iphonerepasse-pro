providers = ["node"]

[variables]
NIXPACKS_NODE_VERSION = "22.13.0"

[phases.install]
cmds = ["npm install -g npm@11.14.1", "npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
